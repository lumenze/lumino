// ── Lumino Demo Injector — Content Script ─────────────────────────────────
// Minimal content script. Recording state persistence is handled entirely
// via sessionStorage (written by injected SDK code) + webNavigation listener
// (in background.js). This file is kept for potential future use.
